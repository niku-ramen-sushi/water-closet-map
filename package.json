{
  "scripts": {
    "build": "npm run setup --prefix frontend && npm run setup --prefix backend",
    "start": "npm run start --prefix backend"
  },
  "dependencies": {
    "prettier": "^3.3.3"
  }
}
